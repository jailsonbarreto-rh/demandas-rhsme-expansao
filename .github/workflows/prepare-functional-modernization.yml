name: Temporary modernization cleanup marker

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary file removed by the registered modernization workflow."
