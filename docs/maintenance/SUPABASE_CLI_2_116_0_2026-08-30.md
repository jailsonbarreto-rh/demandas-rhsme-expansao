# Supabase CLI / Action — 30 de agosto de 2026

## Mudança

- `supabase/setup-cli@v2.1.1` → `supabase/setup-cli@v3.0.0`;
- Supabase CLI fixado em `2.116.0`;
- verificação explícita da versão permanece no workflow.

## Critério

A atualização só pode ser integrada se o workflow `Supabase local migration gate` concluir:

- inicialização do Supabase efêmero;
- replay das migrations desde o Ciclo 3;
- invariantes de Ciclo 3, Ciclo 4, R4, E4 e RPCs;
- ausência de `pg_net`;
- replay completo do banco a partir do zero.

Nenhuma migration é criada ou aplicada ao Supabase Production.
